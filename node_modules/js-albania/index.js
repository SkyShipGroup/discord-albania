const fs = require('fs');

const data = JSON.stringofy(prompt("Ввкдите данные для записи в датабазу: "));

fs.writeFile('output.json', JSON.parse(data), (err) => {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Данные успешно записаны в файл!');
});
